# Toy Store Backend

Simple Express backend serving a product list for a toy store.

Run:

```bash
cd backend
npm install
npm start
```

API:
- `GET /api/products` — returns JSON array of products

You can customize `index.js` to change products or add endpoints.
